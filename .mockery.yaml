with-expecter: true
issue-845-fix: true
resolve-type-alias: false

packages:
  github.com/Chaika-Team/ChaikaGoods/internal/repository:
    config:
      mockname: "Mock{{.InterfaceName}}"
      filename: "{{.InterfaceName}}.mock.go"
      dir: "internal/repository/mocks"
      outpkg: "mocks"
      inpackage: false
    interfaces:
      GoodsRepository:
      # Дополнительные настройки интерфейса, если необходимо
